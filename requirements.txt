# Môi trường Python cho project (pip + venv).
# Version pin cố định để tái lập (reproducibility). Cài: pip install -r requirements.txt
pandas==3.0.3
pyarrow==24.0.0
numpy==2.4.6
duckdb==1.5.3
pyyaml==6.0.3
pytest==9.1.0
