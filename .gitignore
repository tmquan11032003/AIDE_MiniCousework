# Python
__pycache__/
*.py[cod]
.venv/
.venv-feast/
venv/
env/
.ipynb_checkpoints/

# Feast local stores (regenerable)
feast/feature_repo/data/*.db

# Data outputs / generated artifacts (giữ code generator, không commit dữ liệu sinh ra)
data/
*.parquet
*.avro
*.ndjson
*.log
duckdb/
*.duckdb

# Secrets / config local
.env
*.local
# Ngoại lệ: docker/.env chỉ chứa image tag + creds dummy local (không phải secret thật)
!docker/.env

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Claude Code local settings (giữ CLAUDE.md + rules + agents, bỏ settings cá nhân máy)
.claude/settings.local.json
